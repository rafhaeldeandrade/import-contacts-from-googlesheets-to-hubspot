import { PslRetrieveWebsiteDomain } from '@/infra/psl-retrieve-website-domain'

describe('PslRetrieveWebsiteDomain', () => {
  it('should be defined', () => {
    expect(new PslRetrieveWebsiteDomain()).toBeDefined()
  })
})
